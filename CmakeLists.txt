cmake_minimum_required(VERSION 3.28)
project(lab_6)

set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${CMAKE_CURRENT_SOURCE_DIR}/cmake_modules")

include_directories(${CMAKE_CURRENT_SOURCE_DIR})
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/postfix/postfixlib)
include_directories(tablichka)
add_subdirectory(gtest)
add_subdirectory(polinom)  
add_subdirectory(postfix)
add_subdirectory(tablichka)
include_directories(tablichka/Tree)
include_directories(tablichka/HashChain)
include_directories(tablichka/HashList)
include_directories(tablichka/LinerOnArray)
include_directories(tablichka/LinearOnList)
include_directories(tablichka/OrderedOnArray)

add_executable(lab_6 main.cpp maneg.h)

target_link_libraries(polinom PRIVATE gtest)  
target_link_libraries(postfix PRIVATE gtest)   
target_link_libraries(stack PRIVATE gtest)  
target_link_libraries(tablichka PRIVATE gtest)   
